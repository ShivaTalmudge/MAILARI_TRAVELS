{
  "name": "mailari-travels-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Mailari Travels — Complete Travel & Fleet Management Platform",
  "scripts": {
    "postinstall": "cd client && npm install --include=dev && cd ../server && npm install --include=dev && cd .. && npm run build",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "build": "npm run build:server && npm run build:client",
    "build:client": "cd client && npm run build",
    "build:server": "cd server && npm run build",
    "start": "cd server && npm run start",
    "db:migrate": "cd server && npm run db:migrate",
    "db:migrate:prod": "cd server && npm run db:migrate:prod",
    "db:seed:dev": "cd server && npm run db:seed:dev",
    "db:create-admin": "cd server && npm run db:create-admin",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "test": "cd server && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
